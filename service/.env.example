# Contains paths only. Real secrets live outside the repository.
MM02_CONFIG=/etc/mesemondo/config.json
PYTHONPATH=/opt/mesemondo/current/service
PYTHONDONTWRITEBYTECODE=1
# For maintenance/backup use peer auth or a chmod 0600 .pgpass outside the repo.
# PGPASSFILE=/etc/mesemondo/secrets/pgpass
